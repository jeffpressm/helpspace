import React from 'react';
import classNames from 'classnames/bind';

import styles from './Empty.module.scss';

const cx = classNames.bind(styles);

const EmptyGive = ({}) => <div>Empty Give</div>;

export default EmptyGive;
